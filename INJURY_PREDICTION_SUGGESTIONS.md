| 预测目标 | 标签定义 | 任务类型 | 难度 |
|----------|----------|----------|------|
| **未来是否会受伤** | 某段时间内是否发生至少一次伤病（二分类） | 分类 | ⭐ 推荐起步 |
| **伤病严重程度** | 缺阵天数 / 缺阵场次（连续值或分档） | 回归 / 有序分类 | ⭐⭐ |
| **下次伤病类型** | 使用 keyword_to_category 归为几大类 | 多分类 | ⭐⭐ |
| **伤病复发风险** | 同一球员、同一伤情类别是否再次发生 | 二分类（可结合序列） | ⭐⭐⭐ |


# 伤病预测技术文档：「未来是否会受伤」二分类模型

## 一、任务定义

| 项目 | 说明 |
|------|------|
| **预测目标** | 未来是否会受伤（二分类） |
| **业务含义** | 给定**指定球员**与**指定天数（预测窗口）**，预测该球员在该天数内是否会发生**至少一次**伤病 |
| **任务类型** | 二分类（Binary Classification） |
| **标签定义** | 在预测窗口内发生至少一次伤病 → 1（受伤）；否则 → 0（不受伤） |

---

## 二、数据源与字段说明

### 2.1 数据文件

- **路径**：`./saved/players_filled.csv`
- **来源**：PART3 分析与可视化阶段已完成的清洗与填充（如身高缺失值已处理）

### 2.2 字段说明

| 列名 | 类型 | 说明 |
|------|------|------|
| `name` | str | 球员姓名（唯一标识之一） |
| `position` | str | 场上位置（如 Goalkeeper, Centre-Back, Central Midfield） |
| `height/m` | float | 身高（米），已填充 |
| `nationality` | str | 国籍 |
| `injury_history` | str | 伤病史序列，**见下节编码规则** |
| `apparence` | str | 出场数据，格式为 `"(场次, 总分钟)"`，如 `"(495, 44035)"` |

### 2.3 `injury_history` 编码规则（重要）

- 存储为**数字列表的字符串**，例如：`"[-1, 30, 397, 3, 785, 17, ...]"`
- 含义：按时间顺序**交替**表示「健康天数」与「伤病天数」：
  - **偶数下标（0, 2, 4, …）**：健康期天数
  - **奇数下标（1, 3, 5, …）**：伤病期天数（缺阵天数）
- 约定：
  - 首项一般为 `-1`，表示**从有记录起至第一次伤病前**的这段健康期天数未知（或视为“当前”）。
  - 序列从「最早」到「最近」排列，即：`[健康0, 伤1, 健康2, 伤3, …]` 沿时间轴从前到后。

**示例**：`[-1, 30, 397, 3, 785, 17]`  
→ 未知长度健康期 → 伤 30 天 → 健康 397 天 → 伤 3 天 → 健康 785 天 → 伤 17 天。

---

## 三、标签构造（如何从历史得到 0/1）

目标：用历史时间线构造**监督学习样本**，使每个样本对应「某一时刻 + 未来某段窗口内是否受伤」。

### 3.1 基本思路

- 在每条球员的 `injury_history` 时间轴上，取若干**观察起点**（仅从「健康期」起点取，避免“已知正在受伤”的泄漏）。
- 对每个观察起点，给定**预测窗口** \(H\)（单位：天），看从该起点起的 \(H\) 天内是否出现伤病：
  - 若在 \(H\) 天内进入伤病段 → 标签 **1**
  - 若在 \(H\) 天内未进入伤病段（整段都在健康期内）→ 标签 **0**

### 3.2 具体步骤（伪代码）

1. 解析 `injury_history` 得到列表 `series`（偶数位健康天数、奇数位伤病天数）。
2. 从时间轴起点往后遍历：
   - 当前处于「健康段」的起始时，记当前累计天数为 `t`，本段健康天数为 `healthy_days`（若为 -1 可跳过或特殊处理）。
   - 对给定的预测窗口 `horizon_days`（如 30、60、90）：
     - 若 `healthy_days >= horizon_days` 或本段在 \(t + horizon_days\) 内未结束则未受伤 → 标签 0。
     - 若在 \(t\) 到 \(t + horizon_days\) 之间进入伤病段 → 标签 1。
3. 可为同一球员、同一时间点生成**多个样本**（不同 `horizon_days`），或固定 1～2 个常用窗口（如 30 天、90 天）以控制样本量与类别平衡。

### 3.3 注意事项

- **-1 段**：首段健康期为 -1 时，可跳过不生成样本，或仅用「从第一次伤病之后」的时间点生成样本，避免标签模糊。
- **样本平衡**：受伤样本可能偏少，可考虑过采样（如 SMOTE）、欠采样或类别权重（如 `class_weight='balanced'`）。
- **同一球员多样本**：同一球员在不同时间点会产生多条样本，划分训练/验证/测试时建议**按球员划分**（如按 name 或 id 划分），避免同一球员同时出现在训练和测试集中导致信息泄漏。

---

## 四、特征工程

### 4.1 输入维度

模型输入需包含两类信息：

1. **预测窗口（天数）**：即「指定天数」\(H\)，作为特征或条件输入。
2. **球员特征**：在「观察起点」时刻（或当前时刻）可用的静态与历史统计特征。

### 4.2 建议特征列表

| 类别 | 特征名 | 说明 |
|------|--------|------|
| **预测条件** | `horizon_days` | 预测未来多少天内是否受伤（如 30、60、90） |
| **静态** | `position` | 场上位置（One-Hot 或 LabelEncoder） |
| **静态** | `height` | 身高（来自 `height/m`） |
| **静态** | `nationality` | 国籍（高基数可做聚合或编码后降维） |
| **出场** | `total_games`, `total_minutes` | 从 `apparence` 解析 |
| **出场** | `avg_minutes_per_game` | 总分钟/场次 |
| **伤病史（到观察点）** | `num_injuries` | 到该时间点为止的伤病次数 |
| **伤病史** | `total_injury_days`, `avg_injury_days`, `max_injury_days` | 到该时间点的伤病天数的合计/平均/最大 |
| **伤病史** | `last_injury_days` | 最近一次伤病的缺阵天数（若存在） |
| **伤病史** | `days_since_last_injury` | 距上次伤病结束的天数（若存在） |
| **伤病史** | `has_injury_history` | 到该时间点是否有过伤病（0/1） |
| **伤病史（近期）** | `recent_healthy_days_mean/std` | 近期若干健康段天数的均值/标准差 |
| **伤病史（近期）** | `recent_injury_days_mean/std` | 近期若干伤病段天数的均值/标准差 |

说明：若不做「时间点」级细粒度样本，而仅做「球员级」（一个球员只对应少量样本）预测，则上述伤病史特征可改为**全历史**统计（即整条 `injury_history` 的统计），仍保留 `horizon_days` 作为输入。

---

## 五、模型与评估

### 5.1 模型选型建议

- **基线**：逻辑回归（可解释、稳定）。
- **进阶**：随机森林、XGBoost/LightGBM（表格特征常用，可做特征重要性）。
- **评估指标**：准确率、精确率、召回率、F1、**AUC-ROC**（类别不平衡时重点看 AUC 与召回率）。

### 5.2 划分与验证

- **划分**：按**球员**划分训练/验证/测试集（例如 70% / 15% / 15% 的球员），确保同一球员的所有样本只出现在一个集合中。
- **交叉验证**：若按球员划分，可采用按球员 ID 的 GroupKFold 或类似方式，避免泄漏。

### 5.3 类别不平衡

- 使用 `class_weight='balanced'` 或样本重采样。
- 阈值可不用 0.5，根据业务对「漏报（该受伤未预测到）」与「误报」的容忍度调整决策阈值。

---

## 六、接口设计（推理阶段）

### 6.1 输入

- **指定球员**：`player_name` 或 `player_id`（与 CSV 中 `name` 对应）。
- **指定天数**：`horizon_days`（整数，单位：天），表示「未来多少天内是否受伤」。

### 6.2 输出

- **二分类结果**：`will_be_injured`：0 或 1。
- **可选**：`injury_probability`：受伤概率（0～1），便于业务按阈值决策。

### 6.3 使用方式示例

- 训练阶段：用全量 `players_filled.csv` + 上述标签构造与特征工程，训练二分类模型并持久化（如 joblib/pickle）。
- 推理阶段：根据 `player_name` 从 CSV 或缓存中取该球员特征，拼接 `horizon_days`，输入模型得到概率或类别。

---

## 七、实现清单（PART4 建议步骤）

1. 加载 `./saved/players_filled.csv`，解析 `injury_history` 与 `apparence`。
2. 实现「标签构造」逻辑：遍历每条时间线，按健康段起点 + 给定 `horizon_days` 生成 (特征, 标签) 样本。
3. 实现特征工程：静态特征 + 到观察点的伤病史统计 + `horizon_days`。
4. 按球员划分训练/验证/测试集，训练二分类模型（如 LogisticRegression、RandomForest、XGBoost）。
5. 评估：AUC、F1、混淆矩阵；必要时做类别权重或采样。
6. 封装预测接口：输入 `(player_name, horizon_days)`，输出 `will_be_injured` 及可选 `injury_probability`。
7. （可选）保存模型与特征列表，便于在 Notebook 或脚本中复现预测。

---

*文档版本：基于 PART3 产出 `players_filled.csv` 与项目现有 `player.py` 中 Healthy/Injured 与 `injury_history` 约定整理。*
